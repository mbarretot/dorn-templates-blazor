@page "/playground/switch"
@layout PlaygroundLayout

<PageTitle>Playground · Switch</PageTitle>

<ComponentPlayground
    Title="Switch"
    Description="Click the switch in the preview to toggle it; Space does the same when it has focus."
    CodeSnippet="@CodeSnippet"
    ApiParameters="ApiParameters"
>
    <Controls>
        <BoolControl Label="Disabled" @bind-Value="_disabled" />
    </Controls>
    <Preview>
        <div class="flex items-center gap-2">
            <Switch @bind-Value="_checked" Disabled="_disabled" id="airplane-mode" />
            <Label For="airplane-mode">Airplane mode</Label>
        </div>
        <p class="text-sm text-muted-foreground">Checked: @_checked</p>
    </Preview>
</ComponentPlayground>

@code {
    private bool _checked;
    private bool _disabled;

    private string CodeSnippet =>
        $"<Switch @bind-Value=\"_checked\"{(_disabled ? " Disabled=\"true\"" : "")} />";

    private static readonly PlaygroundParameter[] ApiParameters =
    [
        new("Value", "bool", "false", "The bound checked state (two-way bindable)."),
        new("Disabled", "bool", "false", "Disables interaction."),
    ];
}
